## API Contract

The API contract is available as OpenAPI 3.1:

- Raw contract (machine-readable):<br>
  `http://<host>/v3/api-docs`

- Recommended viewers:
  - Swagger Editor (paste JSON)
  - VS Code OpenAPI extension
  - Postman (import URL)