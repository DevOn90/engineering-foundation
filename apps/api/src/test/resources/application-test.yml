spring:
  application:
    name: api
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enable: true
    location: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus